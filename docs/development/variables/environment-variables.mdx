---
title: "Mage environment variables"
sidebarTitle: "Environment variables"
description: "Configure Mage settings through environment variables."
---

## Environment variables in Mage Pro
Environment variables are set from the Mage Pro cluster management portal. Check out the steps and video below on how to create environment variables.

### Adding or modifying environment variables:
    - Click the "Edit / add variables" button
    - On the Environment Variables screen, you can either input a value directly or add a custom environment variable by specifying its key-value pair
    - Click "Save changes" to update the settings.

<iframe width="560" height="315" src="https://www.youtube.com/embed/ZiG67ldzMUE?si=fVCPtZvJfSidpnA_" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>

<Note>
After clicking the "Save changes" button the cluster will automatically restart.
</Note>

## Environment variables in Mage OSS

Environment variables may be set in your Docker container to configure Mage settings. If you’re running Mage using `pip` or `conda`, your local machine’s environment variables
are accessible within the running Mage app.

Environment variables are different from [runtime variables](/getting-started/runtime-variable) or [secrets](/development/variables/secrets).

If you’re running Mage using Docker, you must add the following command line flags:

```bash
-e SOME_VARIABLE_NAME_1=secret_value_1 -e SOME_VARIABLE_NAME_2=secret_value_2
```

The command to run Mage using Docker could look like this:

```bash
docker run -it -p 6789:6789 -v $(pwd):/home/src \
    -e SOME_VARIABLE_NAME_1=secret_value_1 -e SOME_VARIABLE_NAME_2=secret_value_2 \
    mageai/mageai /app/run_app.sh mage start [project_name]
```

## Setting the prefix for Mage URL

You may need to add a prefix to the Mage url. You can configure the Mage server to start
at a prefix to the url by setting the `MAGE_BASE_PATH` environment variable.

Make sure to set the base path without the leading slash. For example, if you want to start Mage at
`http://localhost:6789/base/url/path`, you would configure your environment
variable like this:

`export MAGE_BASE_PATH=base/url/path`

You can also configure the base path separately for client requests and backend routes by setting
the `MAGE_REQUESTS_BASE_PATH` and `MAGE_ROUTES_BASE_PATH` environment variables respectively.

|                           |                                                                                     |
| ------------------------- | ----------------------------------------------------------------------------------- |
| `MAGE_BASE_PATH`          | Set base path for the entire app.                                                   |
| `MAGE_REQUESTS_BASE_PATH` | Set base path for frontend requests and urls, defaults to value of `MAGE_BASE_PATH` |
| `MAGE_ROUTES_BASE_PATH`   | Set base path for backend routes, defaults to value of `MAGE_BASE_PATH`             |

## Default variables

These are the environment variables used in Mage.

| Variable name                    | Description                                                                                                                                                     | Example values                                                           |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `ACTIVE_DIRECTORY_DIRECTORY_ID`  | Set this value to your organization's tenant id to enable signing in with Microsoft account.                                                                    | [More info](/production/authentication/microsoft)                        |
| `ALLOW_DOWNLOADS_OUTSIDE_PROJECT` | Allow signed file downloads for files outside the current project directory.                                                                                    | `0`, `1`                                                                 |
| `AUTHENTICATION_MODE`            | Set the authentication mode in Mage. Omitting the variable will default to basic password authentication.                                                       | `ldap`                                                                   |
| `AI_SIDEKICK` | **(Mage Pro only.)** Controls AI Sidekick, which defaults to enabled when this variable is unset. Set it to `0` to hide AI chat entry points and reject Sidekick backend requests, including runtime runs, model and proxy session tokens, and voice transcription; set it to `1`, `true`, or `t` (case-insensitive) to enable Sidekick explicitly. | `1` (default), `0` |
| `AUTOPILOT_LINEAGE_ENABLED` | **(Mage Pro only.)** Enable Autopilot-managed data lineage hooks for the Mage Pro cluster. Project events still require `lineage.enabled: true` in project `metadata.yaml`. [Data lineage](/observability/data-lineage) | `1` |
| `AUTOPILOT_LINEAGE_API_KEY` | **(Mage Pro only.)** Optional additional service credential sent to Autopilot with lineage requests. [Data lineage](/observability/data-lineage) | `stored-in-secret-manager` |
| `AUTOPILOT_LINEAGE_API_URL` | **(Mage Pro only.)** Optional direct Autopilot lineage service base URL. When unset, Mage Pro routes lineage through the Mage Pro API gateway configured by `MAGE_PRO_API_ENDPOINT`. Do not put it in project code. [Data lineage](/observability/data-lineage) | `https://autopilot.example.com` |
| `AUTOPILOT_LINEAGE_API_TOKEN` | **(Mage Pro only.)** Secret service token for direct Mage Pro to Autopilot lineage requests. Not required when lineage uses the Mage Pro API gateway. [Data lineage](/observability/data-lineage) | `stored-in-secret-manager` |
| `AUTOPILOT_LINEAGE_EVENT_TIMEOUT_SECONDS` | **(Mage Pro only.)** Timeout for a single outbound Autopilot lineage request. [Data lineage](/observability/data-lineage) | `5` |
| `AUTOPILOT_LINEAGE_WORKSPACE_UUID` | **(Mage Pro only.)** Durable Mage Pro workspace identity used to scope lineage events. Falls back to `MAGE_AUTOPILOT_WORKSPACE_ID`, then `MAGE_WORKSPACE_UUID`. [Data lineage](/observability/data-lineage) | `workspace-uuid` |
| `AUTOPILOT_LINEAGE_FEATURE_FLAG` | **(Mage Pro only.)** Optional project `features` key or global boolean gate applied after `lineage.enabled`. [Data lineage](/observability/data-lineage) | `data_lineage`, `true`, `false` |
| `AUTOPILOT_LINEAGE_MAX_FILE_BYTES` | **(Mage Pro only.)** Maximum source-file size included in a lineage snapshot. [Data lineage](/observability/data-lineage) | `524288` |
| `AUTOPILOT_LINEAGE_MAX_SOURCE_SCAN_ENTRIES` | **(Mage Pro only.)** Maximum filesystem entries inspected while discovering allowlisted project source files for a lineage snapshot. [Data lineage](/observability/data-lineage) | `50000` |
| `AUTOPILOT_LINEAGE_MAX_SNAPSHOT_OWNERS` | **(Mage Pro only.)** Maximum owners listed in a lineage snapshot manifest. [Data lineage](/observability/data-lineage) | `10000` |
| `AUTOPILOT_LINEAGE_MAX_SOURCE_BYTES` | **(Mage Pro only.)** Maximum total redacted source bytes included in one lineage snapshot. [Data lineage](/observability/data-lineage) | `5242880` |
| `AUTOPILOT_LINEAGE_ROUTINE_ID` | **(Mage Pro only.)** Logical Mage Pro lineage caller sent to Autopilot. [Data lineage](/observability/data-lineage) | `mage-pro-lineage` |
| `AUTOPILOT_LINEAGE_SNAPSHOT_ENABLED` | **(Mage Pro only.)** Set to `0` to refuse all lineage source-snapshot creation and read requests. [Data lineage](/observability/data-lineage) | `0`, `1` |
| `AUTOPILOT_LINEAGE_SNAPSHOT_GRANT_AUDIENCE` | **(Mage Pro only.)** Expected audience for Autopilot source-snapshot grants. [Data lineage](/observability/data-lineage) | `mage-pro-lineage-snapshot` |
| `AUTOPILOT_LINEAGE_SNAPSHOT_GRANT_ISSUER` | **(Mage Pro only.)** Expected issuer for Autopilot source-snapshot grants. [Data lineage](/observability/data-lineage) | `autopilot-lineage` |
| `AUTOPILOT_LINEAGE_SNAPSHOT_GRANT_SECRET` | **(Mage Pro only.)** Secret used to verify short-lived Autopilot source-snapshot grants. [Data lineage](/observability/data-lineage) | `stored-in-secret-manager` |
| `AUTOPILOT_LINEAGE_SNAPSHOT_TTL_SECONDS` | **(Mage Pro only.)** Lifetime of a lineage source snapshot manifest; a source-snapshot grant cannot exceed twice this lifetime. [Data lineage](/observability/data-lineage) | `900` |
| `AUTOPILOT_LINEAGE_SNAPSHOT_EXPIRED_RETENTION_SECONDS` | **(Mage Pro only.)** How long an expired snapshot remains only to return a stable `410` before cleanup. [Data lineage](/observability/data-lineage) | `86400` |
| `AUTOPILOT_LINEAGE_TENANT_ID` | **(Mage Pro only.)** Autopilot tenant identity required for lineage event requests. [Data lineage](/observability/data-lineage) | `tenant-uuid` |
| `DATAFRAME_SAMPLE_COUNT_PREVIEW` | Number of lines to display in block sample output preview for dataframes. Not suggested to go over 10 lines (defaults to 10).                                   | 5                                                                        |
| `DEBUG`                          | Turn on "debug" mode. Mage will output more logs that could be useful for debugging issues.                                                                     | 1                                                                        |
| `DEFAULT_EXECUTOR_TYPE`          | The default executor type to run blocks                                                                                                                         | See enum types [here](https://docs.mage.ai/production/configuring-production-settings/compute-resource#2-set-executor-type-and-customize-the-compute-resource-of-the-mage-executor) |
| `K8S_DEFAULT_CONFIG_PATH` | **(Mage Pro only.)** Path to YAML file for default K8s executor config; merged with block/project config. Prefer configuring via Helm `k8sExecutorConfig`. [K8s executor](/production/executors/k8#default-config-file) · [Helm](/production/deploying-to-cloud/using-helm#default-workspace-and-k8s-executor-config) | — |
| `K8S_EXECUTOR_DEFAULT_RESOURCE_REQUESTS_CPU` | **(Mage Pro only.)** Default CPU request for K8s executor jobs (when not specified in config)                                                                           | `250m`                                                                   |
| `K8S_EXECUTOR_DEFAULT_RESOURCE_REQUESTS_MEMORY` | **(Mage Pro only.)** Default memory request for K8s executor jobs (when not specified in config)                                                                     | `512Mi`                                                                  |
| `K8S_EXECUTOR_DEFAULT_RESOURCE_LIMITS_CPU` | **(Mage Pro only.)** Default CPU limit for K8s executor jobs (when not specified in config)                                                                                | `500m`                                                                   |
| `K8S_EXECUTOR_DEFAULT_RESOURCE_LIMITS_MEMORY` | **(Mage Pro only.)** Default memory limit for K8s executor jobs (when not specified in config)                                                                          | `1Gi`                                                                    |
| `K8S_EXECUTOR_MAX_RESOURCE_REQUESTS_CPU` | **(Mage Pro only.)** Maximum CPU request guardrail for K8s executor jobs after env/project/pipeline/block config is merged.                                                | `2000m`                                                                  |
| `K8S_EXECUTOR_MAX_RESOURCE_REQUESTS_MEMORY` | **(Mage Pro only.)** Maximum memory request guardrail for K8s executor jobs after env/project/pipeline/block config is merged.                                          | `4Gi`                                                                    |
| `K8S_EXECUTOR_MAX_RESOURCE_LIMITS_CPU` | **(Mage Pro only.)** Maximum CPU limit guardrail for K8s executor jobs after env/project/pipeline/block config is merged.                                                    | `4000m`                                                                  |
| `K8S_EXECUTOR_MAX_RESOURCE_LIMITS_MEMORY` | **(Mage Pro only.)** Maximum memory limit guardrail for K8s executor jobs after env/project/pipeline/block config is merged.                                              | `8Gi`                                                                    |
| `K8S_EXECUTOR_DEFAULT_SERVICE_ACCOUNT_NAME` | **(Mage Pro only.)** Default service account for K8s executor jobs. See [K8s executor docs](/production/executors/k8#environment-variables-reference) for more details | `default`                                                                |
| `K8S_RESOURCE_USAGE_PROMETHEUS_URL` | **(Mage Pro only.)** Prometheus base URL for K8s executor resource usage summaries. See [Monitoring with Prometheus](/observability/external/prometheus#kubernetes-resource-usage-in-mage-pro). | — |
| `K8S_RESOURCE_USAGE_PROMETHEUS_BEARER_TOKEN` | **(Mage Pro only.)** Bearer token for Prometheus resource usage queries. Takes precedence over basic auth when set. | — |
| `K8S_RESOURCE_USAGE_PROMETHEUS_USERNAME` | **(Mage Pro only.)** Username for Prometheus basic authentication. | — |
| `K8S_RESOURCE_USAGE_PROMETHEUS_PASSWORD` | **(Mage Pro only.)** Password for Prometheus basic authentication. | — |
| `K8S_RESOURCE_USAGE_PROMETHEUS_TIMEOUT_SECONDS` | **(Mage Pro only.)** Timeout for each Prometheus resource usage query. | `5` |
| `K8S_RESOURCE_USAGE_PROMETHEUS_STEP_SECONDS` | **(Mage Pro only.)** Query range step for Prometheus CPU and memory samples. | `15` |
| `K8S_RESOURCE_USAGE_PROMETHEUS_COMPLETION_DELAY_SECONDS` | **(Mage Pro only.)** Delay before retrying Prometheus once for completed K8s executor runs with too few samples. | `K8S_RESOURCE_USAGE_PROMETHEUS_STEP_SECONDS` |
| `K8S_RESOURCE_USAGE_PROMETHEUS_VERIFY_TLS` | **(Mage Pro only.)** Set to `0`, `false`, `no`, or `off` to skip TLS certificate verification for Prometheus resource usage queries. | `true` |
| `WORKSPACE_DEFAULT_CONFIG_PATH` | **(Mage Pro only.)** Path to YAML file for default workspace config; merged with user config. Prefer configuring via Helm `workspaceConfig`. [Workspaces](/guides/developer-ux/workspaces#default-workspace-config-file) · [Helm](/production/deploying-to-cloud/using-helm#default-workspace-and-k8s-executor-config) | — |
| `WORKSPACE_DEFAULT_RESOURCE_REQUESTS_CPU` | **(Mage Pro only.)** Default CPU request for workspace containers (when not specified in container_config). See [Workspaces docs](/guides/developer-ux/workspaces#default-resource-configuration) | `500m` |
| `WORKSPACE_DEFAULT_RESOURCE_REQUESTS_MEMORY` | **(Mage Pro only.)** Default memory request for workspace containers (when not specified in container_config) | `2Gi` |
| `WORKSPACE_DEFAULT_RESOURCE_LIMITS_CPU` | **(Mage Pro only.)** Default CPU limit for workspace containers (when not specified in container_config) | `4` |
| `WORKSPACE_DEFAULT_RESOURCE_LIMITS_MEMORY` | **(Mage Pro only.)** Default memory limit for workspace containers (when not specified in container_config) | `8Gi` |
| `DISABLE_NOTEBOOK_EDIT_ACCESS`   | Setting this variable will disable edit access based on the value. [More information](/production/configuring-production-settings/overview#read-only-access)    | 1                                                                        |
| `DISABLE_TERMINAL`               | Setting this variable will disable access to the terminal.                                                                                                      | 1                                                                        |
| `DYNAMIC_BLOCKS_MAX_CHILD_BLOCKS` | Maximum number of dynamic child block runs a dynamic block may create in one expansion. Leave unset or set to `0` to allow unlimited child runs.                 | `1000`                                                                   |
| `DYNAMIC_BLOCKS_MAX_CHILD_BLOCKS_BEHAVIOR` | Behavior when a dynamic block exceeds `DYNAMIC_BLOCKS_MAX_CHILD_BLOCKS`. Use `fail` to stop the run or `limit` to create only up to the configured maximum. | `fail`, `limit` |
| `DYNAMIC_BLOCKS_MAX_CONCURRENT_CHILD_BLOCKS` | Maximum number of dynamic child block runs that may be queued or running at the same time for one pipeline run. Leave unset or set to `0` to allow unlimited concurrent dynamic child runs. | `25` |
| `ENV`                            | Set the environment of the Mage app. You can use different code or configurations in different environments.                                                    | `dev`, `staging`, `production`/`prod`, `test`                            |
| `GIT_REPO_LINK`                  | [More information](/production/data-sync/git-sync#git-sync-settings-as-environment-variables)                                                                   | See link                                                                 |
| `GIT_REPO_PATH`                  | [More information](/production/data-sync/git-sync#git-sync-settings-as-environment-variables)                                                                   | See link                                                                 |
| `GIT_USERNAME`                   | [More information](/production/data-sync/git-sync#git-sync-settings-as-environment-variables)                                                                   | See link                                                                 |
| `GIT_EMAIL`                      | [More information](/production/data-sync/git-sync#git-sync-settings-as-environment-variables)                                                                   | See link                                                                 |
| `GIT_AUTH_TYPE`                  | [More information](/production/data-sync/git-sync#git-sync-settings-as-environment-variables)                                                                   | See link                                                                 |
| `GIT_BRANCH`                     | [More information](/production/data-sync/git-sync#git-sync-settings-as-environment-variables)                                                                   | See link                                                                 |
| `GIT_SYNC_ON_PIPELINE_RUN`       | [More information](/production/data-sync/git-sync#git-sync-settings-as-environment-variables)                                                                   | See link                                                                 |
| `GIT_SYNC_ON_START`              | [More information](/production/data-sync/git-sync#git-sync-settings-as-environment-variables)                                                                   | See link                                                                 |
| `GIT_SSH_PUBLIC_KEY`             | [More information](/production/data-sync/git-sync#git-sync-settings-as-environment-variables)                                                                   | See link                                                                 |
| `GIT_SSH_PRIVATE_KEY`            | [More information](/production/data-sync/git-sync#git-sync-settings-as-environment-variables)                                                                   | See link                                                                 |
| `GIT_ACCESS_TOKEN`               | [More information](/production/data-sync/git-sync#git-sync-settings-as-environment-variables)                                                                   | See link                                                                 |
| `HIDE_ENV_VAR_VALUES`            | Filter out environment variables in notebook output.                                                                                                            | 1                                                                        |
| `JUPYTER_KERNEL_IDLE_TIMEOUT_SECONDS` | **(Mage Pro only.)** Number of seconds an inactive per-user Jupyter kernel can stay alive after its websocket clients disconnect. Used when `MULTI_USER_JUPYTER_KERNELS=1`. | `1800` |
| `JUPYTER_KERNEL_MAX_PER_SERVER` | **(Mage Pro only.)** Maximum number of per-user Jupyter kernels to keep on one Mage server. When the limit is reached, Mage evicts idle kernels first. Used when `MULTI_USER_JUPYTER_KERNELS=1`. | `50` |
| `JWT_SECRET`                     | Modify the json web token secret used for encoding and decoding access tokens. Defaults to `materia`.                                                           | `your_jwt_secret`                                                        |
| `JWT_DOWNLOAD_SECRET`            | Modify the json web token secret used for encoding and decoding download tokens. Defaults to randomly generated string value. Use an explicit value in horizontally scalable deployments, e.g.: kubernetes, cloud deployments | `your_jwt_secret`    |
| `LDAP_ADMIN_USERNAME`            | [More information](/production/authentication/overview#ldap)                                                                                                    | See link                                                                 |
| `LDAP_SERVER`                    | [More information](/production/authentication/overview#ldap)                                                                                                    | See link                                                                 |
| `LDAP_BIND_DN`                   | [More information](/production/authentication/overview#ldap)                                                                                                    | See link                                                                 |
| `LDAP_BIND_PASSWORD`             | [More information](/production/authentication/overview#ldap)                                                                                                    | See link                                                                 |
| `LDAP_BASE_DN`                   | [More information](/production/authentication/overview#ldap)                                                                                                    | See link                                                                 |
| `LDAP_AUTHENTICATION_FILTER`     | [More information](/production/authentication/overview#ldap)                                                                                                    | See link                                                                 |
| `LDAP_AUTHORIZATION_FILTER`      | [More information](/production/authentication/overview#ldap)                                                                                                    | See link                                                                 |
| `LDAP_GROUP_FIELD`               | [More information](/production/authentication/overview#ldap)                                                                                                    | See link                                                                 |
| `LDAP_ROLES_MAPPING`             | [More information](/production/authentication/overview#ldap)                                                                                                    | See link                                                                 |
| `LDAP_DEFAULT_ACCESS`            | [More information](/production/authentication/overview#ldap)                                                                                                    | See link                                                                 |
| `LOGS_DIR_PATH`                  | Override the logs directory path. Default path is the variables_dir path (e.g. `/home/src/mage_data/`)                                                          | `/home/src/logs_dir`                                                     |
| `MAGE_ACCESS_TOKEN_EXPIRY_TIME`  | [More information](/production/authentication/overview#ldap)                                                                                                    | See link                                                                 |
| `MAGE_AGENT_SIDEKICK_ALLOWED_INPUT_ROOTS` | **(Mage Pro only.)** Extra local file roots, separated by the operating system path separator, that internal Sidekick media tools may read from for audio, image, and video inputs. | `/home/src/shared_media:/mnt/media`                                      |
| `MAGE_AGENT_SIDEKICK_MEDIA_ENABLE_VIDEO_DELETE` | **(Mage Pro only.)** Set to `1` to register the internal `sidekick_delete_video` tool for Mage Pro AI Chat Sidekick media sessions. Leave unset to hide the delete tool. | `1`                                                                      |
| `MAGE_AGENT_SIDEKICK_MEDIA_OUTPUT_DIR` | **(Mage Pro only.)** Directory where internal Sidekick media tools save generated images, speech audio, and downloaded videos. Mage Pro AI Chat sets this automatically for each Codex home. | `/home/src/.mage/ai/codex_chat/<session>/media`                         |
| `MAGE_BASE_PATH`                 | The base path or prefix of the Mage server url. [More information](/getting-started/setup#setting-prefix-for-mage-url)                                          | `base/path`                                                              |
| `MAGE_REQUESTS_BASE_PATH`        | [More information](/getting-started/setup#setting-prefix-for-mage-url)                                                                                          | `base/path`                                                              |
| `MAGE_ROUTESBASE_PATH`           | [More information](/getting-started/setup#setting-prefix-for-mage-url)                                                                                          | `base/path`                                                              |
| `MAGE_CACHE_DIRECTORY`           | The directory Mage uses for storing and retrieving temporary data used in the UI.                                                                               | `/root/.mage_data/default_repo/.cache`                                   |
| `MAGE_CODEX_CHAT_ATTACHMENT_MAX_BYTES` | **(Mage Pro only.)** Maximum size, in bytes, for one Mage Pro AI Chat attachment.                                                                                             | `1073741824`                                                             |
| `MAGE_CODEX_CHAT_ATTACHMENT_MAX_SESSION_BYTES` | **(Mage Pro only.)** Maximum total attachment size, in bytes, allowed in one Mage Pro AI Chat session.                                                                  | `2147483648`                                                             |
| `MAGE_CODEX_CHAT_ARCHIVE_ATTACHMENT_MAX_BYTES` | **(Mage Pro only.)** Maximum size, in bytes, for one Mage Pro AI Chat browser-zipped folder archive attachment.                                                       | `1073741824`                                                             |
| `MAGE_CODEX_CHAT_ARCHIVE_ATTACHMENT_MAX_SESSION_BYTES` | **(Mage Pro only.)** Maximum total attachment size, in bytes, allowed in one Mage Pro AI Chat session when the new attachment is a browser-zipped folder archive. | `2147483648`                                                             |
| `MAGE_CODEX_CHAT_ATTACHMENT_CONTENT_PREVIEW_BYTES` | **(Mage Pro only.)** Maximum bytes of text content from one Mage Pro AI Chat attachment that Codex receives in the uploaded-file manifest.                              | `16384`                                                                  |
| `MAGE_CODEX_CHAT_ATTACHMENT_CONTENT_PREVIEW_TOTAL_BYTES` | **(Mage Pro only.)** Maximum total bytes of text attachment content previews sent to Codex for one chat turn.                                                        | `65536`                                                                  |
| `MAGE_CODEX_CHAT_APP_SERVER` | **(Mage Pro only.)** Deprecated compatibility marker for Mage Pro AI Chat. Sidekick now always requires the Codex app-server transport; if app-server cannot start, Mage reports the runtime/deployment error instead of silently falling back to the legacy runner and losing subagent detail ownership. | `required` |
| `MAGE_CODEX_CHAT_ACTIVE_RUN_JANITOR_LOCK_TIMEOUT_SECONDS` | **(Mage Pro only.)** Maximum seconds a Mage Pro AI Chat active-run cleanup pass waits for the shared janitor lock before skipping that pass. | `0` |
| `MAGE_CODEX_CHAT_ACTIVE_RUN_JANITOR_TTL_SECONDS` | **(Mage Pro only.)** Minimum file age, in seconds, before Mage Pro AI Chat cleanup can remove stale active-run JSON files, orphan active-run lock files, or stale atomic temp files. Fresh active work is always skipped. | `600` |
| `MAGE_CODEX_CHAT_MAGE_API_KEY`   | **(Mage Pro only.)** API key the Mage Pro AI Chat runner passes to Mage agent calls back into the current Mage API. Falls back to `MAGE_AGENT_CODEX_API_KEY` or `MAGE_API_KEY`.      | `your_api_key`                                                           |
| `MAGE_CODEX_CHAT_MAGE_API_KEY_SECRET` | **(Mage Pro only.)** Mage secret name that stores the API key for AI Chat runner callbacks into the current Mage API.                                                         | `MAGE_API_KEY`                                                           |
| `MAGE_CODEX_CHAT_MAGE_API_URL`   | **(Mage Pro only.)** Base Mage API URL Mage Pro AI Chat uses to resolve the current workspace URL and base path. Internal Mage agent MCP callbacks derive same-container local API settings from this value, call the local API origin first, and retry the alternate base-path form after a 404. Falls back to `MAGE_CODEX_CHAT_CLUSTER_URL`, local server settings, or the cluster URL. | `https://your-mage-url`                                           |
| `MAGE_CODEX_CHAT_MAGE_API_HOST`  | **(Mage Pro only.)** Same-container host for Mage Pro AI Chat MCP callbacks into the current Mage API. MCP callbacks allow only `localhost`, `127.0.0.1`, `0.0.0.0`, or `::1`; remote host values fall back to `127.0.0.1`. | `127.0.0.1`                                                       |
| `MAGE_CODEX_CHAT_MAGE_API_PORT`  | **(Mage Pro only.)** Same-container port for Mage Pro AI Chat MCP callbacks into the current Mage API. Falls back to `MAGE_PORT`, `PORT`, or `6789`.                                           | `6789`                                                            |
| `MAGE_CODEX_CHAT_MAGE_API_SCHEME` | **(Mage Pro only.)** Same-container URL scheme for Mage Pro AI Chat MCP callbacks into the current Mage API.                                                                                  | `http`                                                            |
| `MAGE_CODEX_CHAT_IMAGE_PREVIEW_SECRET` | **(Mage Pro only.)** Secret used to sign short-lived Mage Pro AI Chat image preview URLs. Falls back to `JWT_SECRET` when unset.                                           | `your_preview_secret`                                                    |
| `MAGE_CODEX_CHAT_MAX_CONCURRENT_RUNS` | **(Mage Pro only.)** Maximum number of Codex chat turns that can run at the same time in one Mage process. Leave unset or set to `0` for unlimited.                              | unset                                                                      |
| `MAGE_CODEX_CHAT_MODEL`          | **(Mage Pro only.)** Default model used by Mage Pro AI Chat when a chat request does not override it. Falls back to `MAGE_CODEX_DEFAULT_MODEL` when unset.                           | `gpt-5.5`                                                                |
| `MAGE_CODEX_CHAT_OPENAI_API_KEY` | **(Mage Pro only.)** Legacy direct Codex API key setting. Mage Pro AI Chat uses the Mage Pro API proxy for model authentication and scrubs this value from the Codex child process. | `unused for AI Chat proxy auth`                                          |
| `MAGE_CODEX_CHAT_OPENAI_API_KEY_SECRET` | **(Mage Pro only.)** Legacy Mage secret name for direct Codex API key lookup. Mage Pro AI Chat proxy authentication does not use this secret for model requests.            | `OPENAI_API_KEY`                                                         |
| `MAGE_CODEX_CHAT_PROGRESS_HEARTBEAT_SECONDS` | **(Mage Pro only.)** Seconds between Mage Pro AI Chat progress heartbeat events while the agent is running without a new tool, command, or final-response event. | `1` |
| `MAGE_CODEX_CHAT_RATE_LIMIT_RETRY_MAX_SECONDS` | **(Mage Pro only.)** Maximum time Mage Pro AI Chat waits and retries a Codex run after a model rate-limit response before showing the rate-limit error. Set to `0` to disable automatic retry. | `30`                                                                     |
| `MAGE_CODEX_CHAT_RATE_LIMIT_RETRY_INITIAL_DELAY_SECONDS` | **(Mage Pro only.)** Initial delay before retrying a Codex run after a model rate-limit response. Mage increases the delay on later attempts up to `MAGE_CODEX_CHAT_RATE_LIMIT_RETRY_MAX_DELAY_SECONDS`. | `5`                                                                      |
| `MAGE_CODEX_CHAT_RATE_LIMIT_RETRY_MAX_DELAY_SECONDS` | **(Mage Pro only.)** Maximum delay between automatic Codex run retry attempts after a model rate-limit response.                                                       | `30`                                                                     |
| `MAGE_CODEX_CHAT_REASONING_EFFORT` | **(Mage Pro only.)** Default reasoning effort used by Mage Pro AI Chat when a chat request does not override it. The AI Chat execution-settings menu takes precedence for that chat. `ultra` is accepted as an alias for Codex CLI's highest supported value, `xhigh`. | `low`, `medium`, `high`, `xhigh`, `ultra`                                |
| `MAGE_CODEX_CHAT_SPEED`          | **(Mage Pro only.)** Default Codex speed used by Mage Pro AI Chat when a chat request does not override it. The AI Chat execution-settings menu takes precedence for that chat.       | `standard`, `fast`                                                       |
| `MAGE_CODEX_CHAT_SERVICE_TIER`   | **(Mage Pro only.)** Default service tier used by Mage Pro AI Chat when a chat request does not override it.                                                                         | `priority`                                                               |
| `MAGE_CODEX_CHAT_SERVER_LOGGING` | **(Mage Pro only.)** Enables structured server-side AI Sidekick logs for turn lifecycle, app-server requests, proxy API probes and session requests, MCP URL shape, timings, sanitized errors, and terminal cleanup. Set to `0`, `false`, `no`, or `off` to disable. Message bodies, prompts, tokens, and secrets are not logged. | `true` |
| `MAGE_CODEX_CHAT_SERVER_LOG_VALUE_MAX_CHARS` | **(Mage Pro only.)** Maximum characters retained for any single structured AI Sidekick log value before truncation. | `500` |
| `MAGE_CODEX_LAUNCH_MAX_STRING_BYTES` | **(Mage Pro only.)** Maximum bytes allowed for one Codex process argument or environment-variable assignment before Mage reports a structured launch-size error. Defaults to 128 KiB. | `131072`                                                                 |
| `MAGE_CODEX_LAUNCH_MAX_TOTAL_BYTES` | **(Mage Pro only.)** Maximum combined bytes allowed for the Codex command arguments and child-process environment before Mage reports a structured launch-size error. When unset, Mage derives a safe limit from the operating system process-argument limit. | `1048576`                                                                |
| `MAGE_CODEX_DEFAULT_MODEL`       | **(Mage Pro only.)** Default Codex model used by Mage Pro AI Chat and Mage agent MCP sessions when a more specific chat model is not configured. Spawned Codex sessions inherit this value. | `gpt-5.5`                                                                |
| `MAGE_CODEX_MODEL_AUTH_MODE`     | **(Mage Pro only.)** Mage Pro AI Chat uses proxy authentication for Codex model requests. Direct mode values are ignored by the AI Chat runtime and backend status check.              | `proxy`                                                                  |
| `MAGE_CODEX_MODEL_PROXY_BASE_URL` | **(Mage Pro only.)** OpenAI-compatible Mage Pro API proxy base URL used by Mage Pro AI Chat. Defaults from the Mage Pro API endpoint when available.                                  | `https://pro-api.mage.ai/api/v1/codex/openai/v1`                         |
| `MAGE_CODEX_MODEL_PROXY_SCRUB_ENV_VARS` | **(Mage Pro only.)** Additional provider credential environment variables, comma or space separated, to remove from the Codex child process in proxy mode. Built-in provider keys are scrubbed automatically. | `CUSTOM_MODEL_API_KEY`                                                   |
| `MAGE_CODEX_CHAT_SESSION_HOME_CLEANUP_INODE_HIGH_WATERMARK` | **(Mage Pro only.)** Inode usage ratio that triggers opportunistic pruning of inactive persistent Codex session homes. Current and active sessions are always skipped. | `0.80` |
| `MAGE_CODEX_CHAT_SESSION_HOME_CLEANUP_INODE_LOW_WATERMARK` | **(Mage Pro only.)** Inode usage ratio where pressure pruning can stop after crossing the high watermark. | `0.60` |
| `MAGE_CODEX_CHAT_SESSION_HOME_CLEANUP_MAX_DIRS` | **(Mage Pro only.)** Maximum inactive persistent Codex session homes to retain before Mage prunes the oldest inactive homes. | `64` |
| `MAGE_CODEX_CHAT_SESSION_HOME_CLEANUP_MAX_REMOVALS_PER_PASS` | **(Mage Pro only.)** Maximum inactive persistent Codex session homes one cleanup pass removes before later cleanup passes continue pruning. | `64` |
| `MAGE_CODEX_CHAT_SESSION_HOME_CLEANUP_SCHEDULE_INTERVAL_SECONDS` | **(Mage Pro only.)** Minimum seconds between process-local background cleanup tasks scheduled by non-ephemeral Mage Pro AI Chat turns. Turn startup does not wait for this cleanup scan. | `300` |
| `MAGE_CODEX_CHAT_SESSION_HOME_CLEANUP_TTL_SECONDS` | **(Mage Pro only.)** Seconds to retain inactive persistent Codex session homes when inode/count pressure is not present. | `86400` |
| `MAGE_SIDEKICK_MODEL_SESSION_CACHE_ENABLED` | **(Mage Pro only.)** Set to `0`, `false`, `no`, or `off` to disable Mage Pro AI Chat caching of short-lived Codex model proxy sessions. | `true` |
| `MAGE_SIDEKICK_MODEL_SESSION_CACHE_SKEW_SECONDS` | **(Mage Pro only.)** Number of seconds before expiry when Mage Pro AI Chat treats a cached Codex model proxy session as stale and mints a replacement. | `60` |
| `MAGE_SIDEKICK_MODEL_SESSION_CIRCUIT_FAILURE_THRESHOLD` | **(Mage Pro only.)** Number of failed Codex model proxy session requests allowed in the circuit window before Mage Pro AI Chat temporarily rejects new model session minting attempts. | `5` |
| `MAGE_SIDEKICK_MODEL_SESSION_CIRCUIT_OPEN_SECONDS` | **(Mage Pro only.)** Number of seconds the Codex model proxy session circuit stays open after the failure threshold is reached. | `30` |
| `MAGE_SIDEKICK_MODEL_SESSION_FILE_LOCK_STALE_SECONDS` | **(Mage Pro only.)** Seconds before a shared Codex model proxy session singleflight lock is treated as stale and recoverable. | `30` |
| `MAGE_SIDEKICK_MODEL_SESSION_SINGLEFLIGHT_ENABLED` | **(Mage Pro only.)** Set to `0`, `false`, `no`, or `off` to disable per-process singleflight coordination for concurrent identical Mage Pro AI Chat Codex model proxy session requests. | `true` |
| `MAGE_SIDEKICK_MODEL_SESSION_SINGLEFLIGHT_WAIT_SECONDS` | **(Mage Pro only.)** Maximum seconds a concurrent Mage Pro AI Chat request waits for an in-flight identical Codex model proxy session request before returning a timeout. | `10` |
| `MAGE_SIDEKICK_MODEL_SESSION_TIMEOUT_SECONDS` | **(Mage Pro only.)** Maximum seconds Mage Pro AI Chat waits while minting a Codex model proxy session through the Mage Pro API proxy. | `8` |
| `MAGE_SIDEKICK_APP_SERVER_IDLE_TTL_SECONDS` | **(Mage Pro only.)** Maximum idle seconds before a worker-scoped warm Codex app-server process is recycled. | `600` |
| `MAGE_SIDEKICK_APP_SERVER_MAX_TURNS` | **(Mage Pro only.)** Maximum compatible Sidekick turns a worker-scoped warm Codex app-server process handles before forced recycle. | `50` |
| `MAGE_SIDEKICK_APP_SERVER_REUSE_ENABLED` | **(Mage Pro only.)** Enables process-scoped warm Codex app-server reuse for compatible Sidekick runtime turns. | `true` |
| `MAGE_SIDEKICK_WORKER_ID` | **(Mage Pro only.)** Optional stable worker identifier used in warm Codex app-server runtime paths. When unset, Mage derives a process-scoped fallback from the host name and process id. Sidekick worker processes set this automatically. | host name plus process id |
| `MAGE_SIDEKICK_CLEANUP_INTERVAL_SECONDS` | **(Mage Pro only.)** Minimum seconds between opportunistic Sidekick runtime cleanup passes. | `60` |
| `MAGE_SIDEKICK_MAX_ACTIVE_RUNS` | **(Mage Pro only.)** Optional maximum active queued or running Sidekick runtime runs admitted across all users in one runtime spool. When the limit is reached, new Sidekick enqueue requests return a retryable `429` instead of holding a web request open. Leave unset or set to `0` for unlimited. | unset |
| `MAGE_SIDEKICK_MAX_ACTIVE_RUNS_PER_USER` | **(Mage Pro only.)** Optional maximum active queued or running Sidekick runtime runs admitted for one user in one runtime spool. When the limit is reached, new Sidekick enqueue requests return a retryable `429` instead of opening another long-lived request. Leave unset or set to `0` for unlimited. | unset |
| `MAGE_SIDEKICK_MAX_WORKERS` | **(Mage Pro only.)** Optional maximum Sidekick worker processes a Mage process can supervise for local runtime-spooled AI Chat turns. Takes precedence over `MAGE_SIDEKICK_WORKER_MAX`. Leave unset or set to `unbounded`, `unlimited`, or `none` for unlimited local fan-out. Set to a positive integer for a fixed cap, or `auto` to cap from the pod CPU quota with a fallback of `4` when no quota is visible. | unset |
| `MAGE_SIDEKICK_MIN_WORKERS` | **(Mage Pro only.)** Minimum Sidekick worker processes to keep available. Takes precedence over `MAGE_SIDEKICK_WORKER_MIN`. | `0` |
| `MAGE_SIDEKICK_PREWARM_ENABLED` | **(Mage Pro only.)** Set to `0`, `false`, `no`, or `off` to disable user-scoped Mage Pro AI Chat runtime prewarming before a Sidekick message is sent. | `true` |
| `MAGE_SIDEKICK_PREWARM_ON_LOGIN` | **(Mage Pro only.)** Legacy compatibility flag for automatic user-scoped Mage Pro AI Chat runtime prewarming. Set to `0`, `false`, `no`, or `off` to disable the authenticated prewarm endpoint and Sidekick intent prewarm trigger without changing the rest of the Sidekick runtime. Ordinary app-load no longer sends a browser prewarm request. | `true` |
| `MAGE_SIDEKICK_PREWARM_MIN_WORKERS` | **(Mage Pro only.)** Minimum Sidekick worker processes to keep available when prewarm requests need runtime capacity. Concurrent warming prewarms can scale beyond this floor, subject to `MAGE_SIDEKICK_MAX_WORKERS` when configured. | `1` |
| `MAGE_SIDEKICK_PREWARM_SYNC_WAIT_MS` | **(Mage Pro only.)** Maximum milliseconds the authenticated prewarm API waits for a prewarm request to report ready before returning `ready`; otherwise it returns the current warming status while the worker continues in the background. | `500` |
| `MAGE_SIDEKICK_PREWARM_TTL_SECONDS` | **(Mage Pro only.)** Seconds to keep ready or warming Sidekick runtime prewarm state before cleanup can expire it. | `1800` |
| `MAGE_SIDEKICK_RUN_MAX_SECONDS` | **(Mage Pro only.)** Maximum seconds a worker allows one Sidekick runtime run to execute before marking it expired. | `3600` |
| `MAGE_SIDEKICK_RUN_TTL_SECONDS` | **(Mage Pro only.)** Seconds to keep terminal Sidekick runtime run files and event logs before cleanup can remove them. | `3600` |
| `MAGE_SIDEKICK_RUNTIME_DIR` | **(Mage Pro only.)** Directory used by Mage Pro AI Chat for runtime coordination files such as run state, event logs, worker claims, and Codex model proxy session cache entries. | System temporary directory plus `mage-sidekick-runtime` |
| `MAGE_SIDEKICK_RUNTIME_ENABLED` | **(Mage Pro only.)** Controls the local ephemeral Mage Pro AI Chat runtime spool so chat turns are admitted to bounded worker processes instead of running inside the webserver event loop. Leave unset, or set to `1`, `true`, `yes`, or `on`, to use the runtime spool. Set to `0`, `false`, `no`, or `off` only to force the legacy inline runner. | `true` |
| `MAGE_SERVER_MAX_BODY_SIZE` | Maximum HTTP request body size, in bytes, accepted by the Mage Tornado server. When unset, Mage sets this to the larger AI Chat attachment limit plus multipart overhead so large uploads can reach the API resource and return normal JSON errors. | `max(MAGE_CODEX_CHAT_ATTACHMENT_MAX_BYTES, MAGE_CODEX_CHAT_ARCHIVE_ATTACHMENT_MAX_BYTES) + 67108864` |
| `MAGE_SERVER_MAX_BUFFER_SIZE` | Maximum HTTP request buffer size, in bytes, accepted by the Mage Tornado server. Defaults to `MAGE_SERVER_MAX_BODY_SIZE`. | `MAGE_SERVER_MAX_BODY_SIZE` |
| `MAGE_SIDEKICK_RUNTIME_SECRET` | **(Mage Pro only.)** Optional secret used to seal short-lived Sidekick runtime credential handoff files. When unset, Mage creates a local `runtime-secret.key` file in `MAGE_SIDEKICK_RUNTIME_DIR` with `0600` permissions. | generated local secret |
| `MAGE_SIDEKICK_WORKER_IDLE_TTL_SECONDS` | **(Mage Pro only.)** Maximum idle seconds before a Sidekick worker process exits when no queued runtime work remains. | `600` |
| `MAGE_SIDEKICK_WORKER_MAX` | **(Mage Pro only.)** Backward-compatible alias for `MAGE_SIDEKICK_MAX_WORKERS`. | unset |
| `MAGE_SIDEKICK_WORKER_MIN` | **(Mage Pro only.)** Backward-compatible alias for `MAGE_SIDEKICK_MIN_WORKERS`. | `0` |
| `MAGE_SIDEKICK_WORKER_QUEUE_PER_WORKER` | **(Mage Pro only.)** Queued Sidekick turns targeted per supervised worker when scaling local runtime workers. | `1` |
| `MAGE_AUTOPILOT_API_URL`         | **(Mage Pro only.)** Legacy direct Autopilot service base URL used by Autopilot service-discovery helpers. Mage Pro Autopilot resource APIs route through the Mage Pro API gateway configured by `MAGE_PRO_API_ENDPOINT` and `MAGE_PRO_API_API_KEY`. | `https://autopilot.mage.ai`                                              |
| `MAGE_AUTOPILOT_BROKER_TOKEN`    | **(Mage Pro only.)** Legacy server-side bearer token for direct Autopilot service requests. Mage Pro Autopilot resource APIs use the Mage Pro API gateway token flow instead. | `your_autopilot_broker_token`                                            |
| `MAGE_AUTOPILOT_GATEWAY_ACCESS_TOKEN` | **(Mage Pro only.)** Optional pre-minted access token for Mage Pro Autopilot resource APIs that call the Mage Pro API gateway. When unset, Mage Pro mints a short-lived gateway token from `MAGE_PRO_API_ENDPOINT` using `MAGE_PRO_API_API_KEY`. | `your_autopilot_gateway_access_token`                                    |
| `MAGE_AUTOPILOT_GATEWAY_SERVICE_USER_ID` | **(Mage Pro only.)** Service user ID used when Mage Pro mints an Autopilot gateway access token. Defaults to `mage-pro-autopilot`.                                 | `mage-pro-autopilot`                                                     |
| `MAGE_AUTOPILOT_PROJECT_UUID`    | **(Mage Pro only.)** Optional project UUID override used to scope Autopilot service requests. When unset, Mage uses the active project's `project_uuid`.           | `project-uuid`                                                           |
| `MAGE_AUTOPILOT_RUNTIME_SYNC_TOKEN` | **(Mage Pro only.)** Shared token required by Autopilot runtime package and runtime run sync endpoints. Keep this value server-side.                            | `your_runtime_sync_token`                                                |
| `MAGE_AUTOPILOT_RUNTIME_SYNC_TOKEN_PREVIOUS` | **(Mage Pro only.)** Previous Autopilot runtime sync token accepted during token rotation.                                                        | `previous_runtime_sync_token`                                            |
| `MAGE_AUTOPILOT_RUNTIME_GRANT_SIGNING_KEY` | **(Mage Pro only.)** Signing key for Autopilot runtime K8s executor grants. Defaults to `MAGE_AUTOPILOT_RUNTIME_SYNC_TOKEN` when unset.             | `your_runtime_grant_signing_key`                                         |
| `MAGE_AUTOPILOT_RUNTIME_MANIFEST_SIGNING_KEY` | **(Mage Pro only.)** Signing key for Autopilot runtime package manifests. Defaults to the runtime sync token when unset.                         | `your_runtime_manifest_signing_key`                                      |
| `MAGE_ENABLE_AUTOPILOT_RUNTIME_EXECUTOR_CONFIG` | **(Mage Pro only.)** Set to `0` or `false` to disable applying signed Autopilot K8s executor configuration from runtime variables for projects with Autopilot enabled. | `1`                                                                      |
| `MAGE_AUTOPILOT_RUNTIME_RUN_TIMEOUT_SECONDS` | **(Mage Pro only.)** Maximum seconds allowed for an Autopilot runtime run subprocess before Mage stops it.                              | `60`                                                                     |
| `MAGE_AUTOPILOT_REQUEST_TIMEOUT_SECONDS` | **(Mage Pro only.)** Timeout, in seconds, for read requests from Mage Pro to the Autopilot service.                                                   | `10`                                                                     |
| `MAGE_AUTOPILOT_UPDATE_TIMEOUT_SECONDS` | **(Mage Pro only.)** Timeout, in seconds, for update requests from Mage Pro to the Autopilot service.                                                 | `10`                                                                     |
| `MAGE_AUTOPILOT_DISPATCH_TIMEOUT_SECONDS` | **(Mage Pro only.)** Timeout, in seconds, for Autopilot dispatch calls such as routine event notifications.                                     | `600`                                                                    |
| `MAGE_AUTOPILOT_RETRY_ATTEMPTS`  | **(Mage Pro only.)** Number of retry attempts for retryable Autopilot service read and dispatch requests.                                             | `3`                                                                      |
| `MAGE_AUTOPILOT_UPDATE_RETRY_ATTEMPTS` | **(Mage Pro only.)** Number of retry attempts for retryable Autopilot service update requests.                                                 | `1`                                                                      |
| `MAGE_OPENAI_PROXY_BASE_URL`     | **(Mage Pro only.)** General OpenAI-compatible Mage Pro API proxy base URL for Sidekick media features such as voice dictation. Defaults to `MAGE_PRO_API_ENDPOINT` plus `/api/v1/openai/v1`. Voice dictation availability still requires server-side `MAGE_PRO_API_API_KEY` and AI Chat mutation permission; raw OpenAI API keys stay in mage-pro-api, not Mage Pro. | `https://pro-api.mage.ai/api/v1/openai/v1` |
| `MAGE_OPENAI_PROXY_ALLOW_PRODUCTION_URL_OVERRIDE` | **(Mage Pro only.)** Allow hosted production Mage Pro clusters to use a custom, non-staging `MAGE_OPENAI_PROXY_BASE_URL` instead of falling back to `pro-api.mage.ai`.                                      | `1`, `true`                                                              |
| `MAGE_OPENAI_PROXY_SESSION_TOKEN_PATH` | **(Mage Pro only.)** Mage Pro API path used to mint short-lived scoped OpenAI proxy tokens for Sidekick media requests.                                                          | `/api/v1/openai_proxy/session_tokens`                                    |
| `MAGE_OPENAI_PROXY_TRANSCRIPTION_MODEL` | **(Mage Pro only.)** Fallback transcription model requested by generalized Mage Pro API OpenAI proxy transcription calls when a caller does not pass a model. Sidekick voice dictation passes `MAGE_SIDEKICK_VOICE_DICTATION_MODEL` instead. | `gpt-4o-transcribe`                                                      |
| `MAGE_SIDEKICK_VOICE_DICTATION_DAILY_MAX_BYTES` | **(Mage Pro only.)** Maximum daily audio bytes accepted per user before Mage Pro refuses new Sidekick voice dictation proxy requests.                                       | `524288000`                                                              |
| `MAGE_SIDEKICK_VOICE_DICTATION_LANGUAGE` | **(Mage Pro only.)** Default language code sent with Sidekick voice dictation transcription requests when the browser does not provide one. Invalid values fall back to English. | `en`                                                                     |
| `MAGE_SIDEKICK_VOICE_DICTATION_MAX_BYTES` | **(Mage Pro only.)** Maximum audio upload size Mage Pro accepts and passes into the scoped OpenAI proxy token budget for Sidekick voice dictation.                              | `26214400`                                                               |
| `MAGE_SIDEKICK_VOICE_DICTATION_MAX_SECONDS` | **(Mage Pro only.)** Maximum client recording duration Mage Pro exposes to Sidekick and passes into the scoped OpenAI proxy token budget for voice dictation.                   | `120`                                                                    |
| `MAGE_SIDEKICK_VOICE_DICTATION_MODEL` | **(Mage Pro only.)** Transcription model requested through the Mage Pro API OpenAI proxy for Sidekick voice dictation.                                                           | `gpt-4o-transcribe`                                                      |
| `MAGE_SIDEKICK_VOICE_DICTATION_MONTHLY_MAX_BYTES` | **(Mage Pro only.)** Maximum monthly audio bytes accepted per user before Mage Pro refuses new Sidekick voice dictation proxy requests.                                    | `10737418240`                                                            |
| `MAGE_SIDEKICK_VOICE_DICTATION_PROMPT` | **(Mage Pro only.)** Prompt sent with Sidekick voice dictation transcription requests. Defaults to preserving Mage, Sidekick, Codex, pipeline, Kubernetes, GitHub, API, and other technical terms, and is capped at 1000 characters. | `Transcribe English voice dictation for Mage AI Sidekick...`             |
| `MAGE_SIDEKICK_VOICE_DICTATION_RATE_LIMIT_PER_MINUTE` | **(Mage Pro only.)** Maximum Sidekick voice dictation transcription attempts accepted per user per minute before Mage Pro returns a rate-limit response.                 | `20`                                                                     |
| `MAGE_DATABASE_CONNECTION_URL`   | Specify the database connection url for orchestration DB. Defaults to local sqlite db.                                                                          | [Example](/production/configuring-production-settings/overview#postgres) |
| `MAGE_DATABASE_APPLICATION_NAME` | Override the PostgreSQL `application_name` used by Mage DB connections. Defaults to a generated `mage:<cluster or project>:<hostname>` value.                    | `mage:production:server-1`                                                |
| `MAGE_DATABASE_IDLE_IN_TRANSACTION_SESSION_TIMEOUT_MS` | PostgreSQL session timeout, in milliseconds, for idle transactions opened by Mage. Set to `0` to disable the per-session timeout. | `120000` |
| `MAGE_DATABASE_LOCK_TIMEOUT_MS`  | Optional PostgreSQL per-session lock wait timeout, in milliseconds, for Mage DB connections. `0` disables this setting.                                         | `0`                                                                      |
| `MAGE_DATABASE_MAX_OVERFLOW`     | Extra PostgreSQL connections a Mage process may open above `MAGE_DATABASE_POOL_SIZE`.                                                                           | `2`                                                                      |
| `MAGE_DATABASE_POOL_RECYCLE`     | Number of seconds before SQLAlchemy recycles an idle PostgreSQL pooled connection.                                                                               | `1800`                                                                   |
| `MAGE_DATABASE_POOL_SIZE`        | Base SQLAlchemy connection pool size per Mage process for PostgreSQL orchestration DB connections. Use small values for shared workspaces.                       | `5`                                                                      |
| `MAGE_DATABASE_POOL_TIMEOUT`     | Number of seconds a Mage request waits for a PostgreSQL pooled connection before failing with a pool exhaustion error.                                           | `10`                                                                     |
| `MAGE_DATABASE_STATEMENT_TIMEOUT_MS` | Optional PostgreSQL per-session statement timeout, in milliseconds, for Mage DB connections. `0` disables this setting.                                      | `0`                                                                      |
| `MAGE_PRO_API_API_KEY`           | **(Mage Pro only.)** Server-side Mage Pro API key used when Mage Pro proxy mode or other Mage Pro API integrations call the shared Mage Pro API service. Mage Pro AI Chat does not pass this key to the Codex child process or Mage MCP tool environment. | `your_mage_pro_api_key`                                                  |
| `MAGE_PRO_API_ENDPOINT`          | **(Mage Pro only.)** Mage Pro API host used by Mage Pro proxy mode and other Mage Pro API integrations. Do not include the URL scheme. Hosted production clusters use `pro-api.mage.ai`; staging hosts such as `pro-api-staging.mage.ai` are for non-production environments. | `pro-api.mage.ai`                                                        |
| `MAGE_PUBLIC_HOST`               | The public host url that can be used to access the Mage app. This value will be used in emails or other notifications.                                          | `http://localhost:6789`                                                  |
| `MAGE_RESTART_STREAMING_PIPELINES_ON_REQUIREMENTS_CHANGE` | **(Mage Pro only.)** When true, running streaming pipelines are restarted after the project `requirements.txt` changes.                                                | 1                                                                        |
| `MAX_PRINT_OUTPUT_LINES`         | The max number of `stdout` lines, such as from `print` statements, included in a message from the websocket server. Defaults to `1000`.                         | `100`                                                                    |
| `MULTI_USER_JUPYTER_KERNELS` | **(Mage Pro only.)** Enable a separate Python Jupyter kernel for each authenticated user in the pipeline editor so multiple users can run blocks at the same time. | `0` |
| `REQUIRE_USER_AUTHENTICATION`    | Enable user authentication in Mage. [More information](/production/authentication/overview)                                                                     | 1                                                                        |
| `RUN_PIPELINE_IN_ONE_PROCESS`    | When True, pipelines will run in one process, unless otherwise specified in the settings for a given pipeline.                                                  | 1
| `SERVER_VERBOSITY`               | [More information](/development/observability/logging#server-logging)                                                                                           | See link                                                                 |
| `SHELL_COMMAND`                  | Set shell command to use for the Mage terminal. Default command is `bash` for macOS/Unix and `cmd` for Windows.                                                 | `bash`, `cmd`, ...                                                       |
| `ULIMIT_NO_FILE`                 | Override the maximum number of open files allowed in Mage processes.                                                                                            | 8192                                                                     |
| `USER_CODE_PATH`                 | Set this path to be `/home/src/{your_project_name}` so that Mage starts your project and installs the packages in requirements.txt when starting the container. | `/home/src/mage_project`                                                 |

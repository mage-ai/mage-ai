---
title: "MCP Tools"
description: "Full reference for every tool your AI assistant can call via mage-agent."
---

import { ProOnly } from '/snippets/pro/only.mdx';

Once mage-agent is connected to your IDE, your AI assistant has access to the following tools. All tools require valid authentication.

<Note>
  In MCP clients that support rendered tool output, mage-agent returns compact
  resource widgets for supported resource actions. Widgets are currently
  available for pipelines, blocks, pipeline runs, block runs, block outputs,
  pipeline schedules, and pipeline triggers. They do not change the tool
  arguments or authentication flow; clients that do not support rendered output
  still receive the normal structured tool response.
</Note>

## Resource Widget Contract

The widget path is an additive MCP Apps enhancement for read-only resource
tools. A widget-enabled tool descriptor includes both `_meta.ui.resourceUri` and
`_meta["openai/outputTemplate"]` pointing at the registered HTML resource. When
the tool succeeds, mage-agent returns:

- `structuredContent.kind = "mage_resource_widget"`
- `structuredContent.schema_version` for renderer compatibility
- `structuredContent.resource` and `structuredContent.action`
- `structuredContent.state` as `ok`, `empty`, `loading`, or `error`
- sanitized `records`, `summary`, `columns`, `filters`, and `pagination`
- optional sanitized `mutation` metadata for create, update, or delete results
- optional sanitized `graph` data for pipeline-oriented detail widgets, using
  normalized `nodes` and `edges` instead of raw pipeline YAML
- `content[0].text` as a JSON fallback for clients that do not render widgets
- `_meta["openai/widgetData"]` with widget-only rendering hints

List and get widgets render the normal table or detail view. Mutating actions
may render the returned record with a mutation summary banner and footer, such as
`Operation: Create`; delete results without a returned record render a compact
success state. The adapter must redact sensitive fields before they enter
`structuredContent`, fallback `content`, widget `_meta`, or activity logs.

Pipeline-oriented widgets may include a compact graph preview when Mage returns
block dependency metadata. The graph shape is:

```json
{
  "nodes": [
    {
      "id": "load_customers",
      "label": "Load customers",
      "type": "data_loader",
      "status": "completed",
      "selected": false
    }
  ],
  "edges": [
    {
      "source": "load_customers",
      "target": "segment_customers"
    }
  ],
  "totalNodes": 12,
  "truncated": false
}
```

Renderers should treat graph data as optional. If it is missing, malformed, or
too large, the widget must fall back to the existing list/detail table without
blocking the tool response.

## Adding Widget Support

To add another Mage resource widget:

1. Add the resource/action pair to `widget_contracts.py`.
2. Add presentation columns, summary fields, empty/error copy, and guidance in
   `widget_registry.py`.
3. Add adapter tests that cover populated, empty, malformed, large-result, and
   secret-bearing payloads.
4. Add renderer coverage in `widgets/resource_widget` and the app-shell preview
   route under `/apps/test/resource-widgets`.
5. Run the MCP protocol tests and an EKS mocked app-shell visual check before
   calling the change QA-ready.

For normal Mage trigger inventory, active trigger counts, API/webhook trigger
lists, run-once schedules, and next run dates, use `pipeline_schedules`. Use
`pipeline_triggers` only when the user explicitly asks for the advanced
`PipelineTriggerResource` config surface scoped to a pipeline.

---

## Codex chat context

<AccordionGroup>
  <Accordion title="current_page_context">
    Read the visible non-chat text that Mage Pro AI Chat captured from the user's current browser page for the active chat turn.

    This tool is only registered when Mage Pro AI Chat starts Codex with captured page context. Standard IDE MCP sessions do not expose it unless `MAGE_AGENT_CURRENT_PAGE_CONTEXT_PATH` points to a valid context sidecar file.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `include_text` | boolean | no | Return the captured page text. Set to `false` to return metadata only. |

    <Note>
      The context is scoped to the authenticated user and chat session, excludes the Codex chat UI, and should only be used for page-relative requests such as "explain this page" or "what am I looking at?"
    </Note>
  </Accordion>
</AccordionGroup>

---

## Sidekick data preview tools

<ProOnly source="sidekick-data-previews" />

<Note>
  These tools are available to Mage Pro AI Chat Sidekick sessions that start
  mage-agent with the internal data preview skill. They use Mage session
  authorization and existing IO profile credentials; tool responses never
  include raw credentials or full row datasets.
</Note>

Sidekick uses these tools to discover queryable IO sources and pipeline block
outputs, create a session-scoped data preview or computed read-only SQL result,
render a `data_table` artifact in AI Chat, and refine that preview through
follow-up prompts or table controls.
For SQL-capable IO profiles, Sidekick can also create a preview from a computed
read-only query result, such as a `SELECT` with `GROUP BY`, joins, or derived
columns, instead of returning only a small markdown table in chat.
Preview refinements create child previews with a `parent_preview_id`, so users
can move back to an earlier preview without rebuilding the original query.
The artifact keeps the original `preview_id` for lineage, updates
`current_preview_id` as the user opens child or sibling previews, and can include
`root_preview_id`, `parent_preview_id`, `status`, `capabilities`, `source_label`,
`query_summary`, `row_count`, and `row_count_mode` so the chat panel can render a
compact summary while the artifact panel streams table windows.
Preview-window row counts are not full table cardinality. When the user asks how
many rows exist in a source table or filtered result, Sidekick should run an
explicit read-only `COUNT(*)` query or use an exact-count preview capability
instead of answering from the number of rows currently loaded in the artifact.
Artifact sort and filter controls refine the source-side preview rather than
reordering only the loaded rows, so large or unindexed remote tables can still
hit the provider timeout while preserving full-dataset semantics.
Set `query_plan.limit` only when the user explicitly asks for a row cap, such as
"top 20", "first 100 rows", "sample 500", or "limit to N". Normal exploration,
source-table previews, date filters, and computed query previews should not add
a safety/default row cap because the preview backend and artifact panel already
fetch bounded windows.

<AccordionGroup>
  <Accordion title="rank_data_candidates">
    Rank matching IO sources, database tables, and pipeline block outputs for a
    natural-language request.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `user_request` | string | yes | User request to resolve into a queryable source |
    | `context` | object | no | Active workspace, project, page, pipeline, block, current preview, and recent-turn hints |
    | `limit` | integer | no | Maximum ranked candidates to return |

    Use the highest-confidence candidate directly when the match is clear. When
    several candidates are plausible, ask the user to choose with the structured
    Sidekick option-selection UI. For broad inventory requests such as "what
    data can we explore?", do not pass a `limit` unless the user asks for a
    capped list. Prefer surfacing queryable IO sources before pipeline or block
    outputs, and when the response is bounded, describe the returned entries as
    examples with the available counts instead of saying only those entries can
    be explored. If a count is marked truncated, phrase it as "at least N"
    instead of a complete total.
  </Accordion>

  <Accordion title="search_queryable_sources">
    Search authorized IO profile sources and integration source or destination
    configs that Sidekick can inspect for data previews.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `query` | string | no | Free-text source search |
    | `context` | object | no | Workspace, project, page, or recent-turn hints |
    | `limit` | integer | no | Maximum sources to return |

    Source metadata includes `selector_ladder`, `missing_selectors`,
    `next_selector`, and `preview_support`. Sidekick should use those fields to
    ask for the next provider-specific selector, such as warehouse, database,
    catalog, project, dataset, schema, or table, instead of guessing defaults.
  </Accordion>

  <Accordion title="inspect_queryable_source">
    Inspect one queryable source and return redacted connection metadata plus
    preview capabilities.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `source_id` | string | yes | Source identifier returned by discovery |

    Use `metadata.preview_support.supported` to distinguish sources that are
    configured and metadata-browseable from sources that can create data viewer
    table previews. Do not create a failed preview artifact for a provider the
    metadata already marks unsupported.
  </Accordion>

  <Accordion title="list_source_warehouses">
    List Snowflake warehouses for a discovered IO source.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `source_id` | string | yes | Source identifier returned by discovery |
    | `limit` | integer | no | Maximum warehouses to return |

    Use this when a Snowflake source has no configured or supplied warehouse.
    Present the returned `warehouse_candidates` as the next required selector
    before listing databases, schemas, tables, or creating a preview.
  </Accordion>

  <Accordion title="list_source_databases">
    List databases, catalogs, or equivalent top-level namespaces for a discovered
    IO source.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `source_id` | string | yes | Source identifier returned by discovery |
    | `warehouse` | string | no | Snowflake warehouse selected for metadata discovery |
    | `limit` | integer | no | Maximum databases or catalogs to return |

    If the source needs another selector first, the tool returns
    `requires_selection` with concrete candidates, such as `warehouse_candidates`.
  </Accordion>

  <Accordion title="list_source_schemas">
    List schemas, datasets, or equivalent namespaces for a discovered IO source.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `source_id` | string | yes | Source identifier returned by discovery |
    | `database` | string | no | Database or catalog selected for metadata discovery |
    | `warehouse` | string | no | Snowflake warehouse selected for metadata discovery |
    | `limit` | integer | no | Maximum schemas or datasets to return |
  </Accordion>

  <Accordion title="search_source_tables">
    Search tables, schemas, or streams inside a discovered IO source.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `source_id` | string | yes | Source identifier returned by discovery |
    | `query` | string | no | Table, schema, stream, or column search text |
    | `database` | string | no | Database name to scope metadata lookup |
    | `schema` | string | no | Database schema or namespace |
    | `warehouse` | string | no | Snowflake warehouse selected for metadata discovery |
    | `limit` | integer | no | Maximum tables to return |

    If warehouse, database, schema, catalog, project, dataset, or table scope is
    ambiguous, the response may include `requires_selection` plus
    `warehouse_candidates`, `database_candidates`, `schema_candidates`, or
    `table_selection_candidates`. Ask the user to choose only that missing
    selector unless their request clearly names one option. Follow the source
    `selector_ladder` and `next_selector` for provider-specific ordering.

    When the response includes `table_candidates`, pass the selected candidate
    directly as `source_ref` to `create_data_preview`; it preserves the IO
    profile, database, schema, table, and project configuration context needed
    for remote SQL previews.
  </Accordion>

  <Accordion title="inspect_source_table">
    Inspect a single table or stream and return redacted schema information that
    can be used to build a preview query.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `source_id` | string | yes | Source identifier returned by discovery |
    | `table_ref` | string or object | yes | Table name or object with `database`, `schema`, and `table` |
    | `warehouse` | string | no | Snowflake warehouse selected for metadata discovery |

    For Snowflake table inspection, pass the same selected warehouse that will be
    used for preview creation.
  </Accordion>

  <Accordion title="search_pipeline_outputs">
    Search pipeline block outputs that can be opened as data previews, including
    Parquet-backed block outputs.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `query` | string | no | Pipeline, block, variable, or output search text |
    | `context` | object | no | Active workspace, project, pipeline, block, or page hints |
    | `limit` | integer | no | Maximum outputs to return |
  </Accordion>

  <Accordion title="inspect_pipeline_output">
    Inspect a specific pipeline block output before creating a preview.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `output_ref` | string or object | yes | Pipeline output source ID or discovery candidate object |
  </Accordion>

  <Accordion title="create_data_preview">
    Create a session-scoped data preview and return compact preview metadata,
    the first row window, and a `data_table` artifact descriptor for AI Chat.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `source_ref` | object | yes | Source reference from discovery or inspection |
    | `query_plan` | object | no | Selected columns, filters, sort, an explicit user-requested limit, or source-specific query details |
    | `artifact_context` | object | no | Optional title and summary hints for the rendered artifact |

    For remote SQL sources, prefer a selected `table_candidate` from
    `search_source_tables` as the `source_ref` instead of rebuilding the source
    reference manually.

    The returned artifact identifies the original preview by `preview_id` and
    the active table by `current_preview_id`; it does not embed the full dataset
    in the chat response. The AI Chat artifact panel reads windowed rows from
    the preview API and updates `current_preview_id` when the user opens a child,
    sibling, parent, or root preview.
  </Accordion>

  <Accordion title="create_source_table_preview">
    Resolve an IO config-backed source and table, then create a renderable data
    preview artifact in one call.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `table` | string | yes | Table name, optionally qualified with database and schema |
    | `source_id` | string | no | Source identifier returned by discovery |
    | `source_query` | string | no | Free-text source search used when `source_id` is not provided |
    | `database` | string | no | Database name to apply to table discovery |
    | `schema` | string | no | Schema or namespace to apply to table discovery |
    | `warehouse` | string | no | Snowflake warehouse selected for table discovery |
    | `query_plan` | object | no | Selected columns, filters, sort, an explicit user-requested limit, or source-specific query details |
    | `artifact_context` | object | no | Optional title and summary hints for the rendered artifact |

    Use this when the user names both a source or profile and a table. If source
    metadata says `preview_support.supported` is false, report that the source
    is configured but not supported by data viewer previews yet instead of
    creating a failed artifact. Some providers can create previews even when
    metadata browsing cannot list tables; pass any provider-specific selectors
    from the `selector_ladder` or a fully qualified table name. If a required
    selector is missing, the tool returns `requires_selection` for that selector
    instead of creating an under-scoped preview. For Snowflake, pass the selected
    warehouse; if none is configured or selected, the tool returns
    `requires_selection` with `warehouse_candidates` instead of creating a
    preview.
  </Accordion>

  <Accordion title="create_sql_query_preview">
    Create a session-scoped data preview for a computed read-only SQL query
    result against an IO config-backed source, then return compact preview
    metadata and a `data_table` artifact descriptor for AI Chat.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `query` | string | yes | Single read-only SQL `SELECT` or `WITH` statement to preview |
    | `source_id` | string | no | Source identifier returned by discovery |
    | `source_query` | string | no | Free-text source search used when `source_id` is not provided |
    | `database` | string | no | Database name to apply to the source context |
    | `schema` | string | no | Schema or namespace to apply to the source context |
    | `warehouse` | string | no | Snowflake warehouse selected for query execution |
    | `artifact_context` | object | no | Optional title and summary hints for the rendered artifact |

    Use this tool when the user asks to see a computed result in the data
    viewer, including aggregations, grouped results, joins, or derived columns.
    Pass provider-specific context from discovery, such as warehouse, catalog,
    database, or schema. If required selector scope is missing, the tool returns
    `requires_selection` instead of creating an under-scoped preview. For
    Snowflake SQL previews, pass a selected warehouse; if none is configured or
    selected, ask for a warehouse before running the query preview. Mage
    validates the SQL as a single read-only statement before creating the
    preview. The returned artifact uses preview IDs and windowed row fetches
    just like table-backed previews; it does not embed the full query result in
    the chat response.
  </Accordion>

  <Accordion title="refine_data_preview">
    Create a child preview from an existing preview by applying a filter, sort,
    column selection, or limit change.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `parent_preview_id` | string | yes | Existing preview to refine |
    | `instruction` | string | no | Natural-language reason for the refinement |
    | `query_delta` | object | no | Filter, sort, select, or limit delta |
    | `artifact_context` | object | no | Optional title and summary hints for the rendered artifact |

    Use this tool for follow-up prompts such as "only USA" or "sort sessions
    desc" and for table filter or sort controls that should preserve lineage.
    Include `limit` in `query_delta` only for an explicit user-requested row cap;
    capped previews render a `Limit N` chip in the artifact controls.
  </Accordion>

  <Accordion title="get_data_preview">
    Read compact metadata for an existing preview and optionally include lineage.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `preview_id` | string | yes | Preview to read |
    | `include_lineage` | boolean | no | Include parent and child preview references |
  </Accordion>

  <Accordion title="fetch_data_preview_window">
    Fetch a bounded row window for a preview.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `preview_id` | string | yes | Preview to read |
    | `start_or_cursor` | integer or string | no | Zero-based starting row offset or source cursor |
    | `limit` | integer | no | Maximum rows to return |
    | `columns` | array | no | Optional column keys to include |
    | `include_rows` | boolean | no | Whether to include row values in the tool response |

    Use this tool sparingly for small summaries. The AI Chat virtual table
    fetches its own visible windows directly from the preview APIs and may
    prefetch nearby windows. Cursor-only sources can scroll forward but may not
    support jumping directly to an arbitrary row.
  </Accordion>

  <Accordion title="cancel_data_preview">
    Cancel or retire a preview request when the user dismisses an in-flight
    preview or starts a replacement query.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `preview_id` | string | yes | Preview to cancel |
    | `reason` | string | no | Optional cancellation reason |
  </Accordion>
</AccordionGroup>

---

## Sidekick media tools

<Note>
  These tools are available only to Mage Pro AI Chat Sidekick sessions that start
  mage-agent in internal mode. Standard IDE MCP sessions do not expose them.
  Tool results never include provider tokens or Mage Pro service credentials.
</Note>

Generated media is saved under the Sidekick media output directory for the
active chat session and turn. Tools that read local files accept only files under
the current workspace, project, data directory, configured allowed input roots,
or the Sidekick media output directory.

The Mage Pro AI Chat runtime sets the Sidekick user, session, turn, and media
output directory automatically. Operators can set
`MAGE_AGENT_SIDEKICK_MEDIA_OUTPUT_DIR` to override the generated media directory,
`MAGE_AGENT_SIDEKICK_ALLOWED_INPUT_ROOTS` to add extra local input roots, and
`MAGE_AGENT_SIDEKICK_MEDIA_ENABLE_VIDEO_DELETE=1` to register
`sidekick_delete_video`. Media files returned by these tools render in AI Chat
through short-lived authenticated preview URLs.

The Codex child process and Mage MCP tool process receive Mage session auth only;
they do not receive the mage-pro-api service key.

<AccordionGroup>
  <Accordion title="sidekick_transcribe_audio">
    Transcribe a local audio file, or supported video file.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `file_path` | string | yes | Path to the local audio or supported video file |
    | `model` | string | no | Transcription model, default `gpt-4o-transcribe` |
    | `language` | string | no | Input language hint |
    | `prompt` | string | no | Optional transcription prompt |
    | `response_format` | string | no | `json`, `text`, `srt`, `verbose_json`, or `vtt`; default `json` |
    | `temperature` | number | no | Sampling temperature |
    | `max_upload_bytes` | integer | no | Upload byte limit for this request |
    | `timeout_seconds` | integer | no | Request timeout |
  </Accordion>

  <Accordion title="sidekick_translate_audio">
    Translate a local audio file to English.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `file_path` | string | yes | Path to the local audio file |
    | `model` | string | no | Translation model, default `gpt-4o-transcribe` |
    | `prompt` | string | no | Optional translation prompt |
    | `response_format` | string | no | `json`, `text`, `srt`, `verbose_json`, or `vtt`; default `json` |
    | `temperature` | number | no | Sampling temperature |
    | `max_upload_bytes` | integer | no | Upload byte limit for this request |
    | `timeout_seconds` | integer | no | Request timeout |
  </Accordion>

  <Accordion title="sidekick_text_to_speech">
    Create speech audio from text and save it locally. The result includes the
    saved path, MIME type, and byte count.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `input` | string | yes | Text to synthesize |
    | `model` | string | no | Speech model, default `gpt-4o-mini-tts` |
    | `voice` | string | no | Voice name, default `alloy` |
    | `instructions` | string | no | Voice or style instructions |
    | `response_format` | string | no | `aac`, `mp3`, `opus`, or `wav`; default `mp3` |
    | `speed` | number | no | Speech speed |
    | `timeout_seconds` | integer | no | Request timeout |
  </Accordion>

  <Accordion title="sidekick_generate_image">
    Generate images from a prompt. Base64 image payloads are persisted to local
    files and redacted from the structured response.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `prompt` | string | yes | Image prompt |
    | `model` | string | no | Image model, default `gpt-image-1` |
    | `n` | integer | no | Number of images, 1-10; default `1` |
    | `size` | string | no | `auto`, `1024x1024`, `1024x1536`, or `1536x1024`; default `1024x1024`. Legacy `1024x1792` and `1792x1024` inputs are normalized to the supported portrait and landscape sizes. |
    | `quality` | string | no | Provider quality option |
    | `background` | string | no | Provider background option |
    | `moderation` | string | no | Provider moderation option |
    | `output_format` | string | no | Provider output format option |
    | `response_format` | string | no | Optional provider response format, `b64_json` or `url`; omit for `gpt-image-*` models |
    | `style` | string | no | Provider style option |
    | `timeout_seconds` | integer | no | Request timeout |
  </Accordion>

  <Accordion title="sidekick_edit_image">
    Edit one or more local images from a prompt. Persisted outputs are returned
    as local file paths.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `prompt` | string | yes | Edit prompt |
    | `image_path` | string | no | Single local input image path |
    | `image_paths` | array | no | Multiple local input image paths |
    | `mask_path` | string | no | Optional local mask image path |
    | `model` | string | no | Image model, default `gpt-image-1` |
    | `n` | integer | no | Number of images, 1-10; default `1` |
    | `size` | string | no | `auto`, `1024x1024`, `1024x1536`, or `1536x1024`; default `1024x1024`. Legacy `1024x1792` and `1792x1024` inputs are normalized to the supported portrait and landscape sizes. |
    | `quality` | string | no | Provider quality option |
    | `background` | string | no | Provider background option |
    | `input_fidelity` | string | no | Provider input fidelity option |
    | `output_format` | string | no | Provider output format option |
    | `response_format` | string | no | Optional provider response format, `b64_json` or `url`; omit for `gpt-image-*` models |
    | `max_upload_bytes` | integer | no | Upload byte limit for this request |
    | `timeout_seconds` | integer | no | Request timeout |

    Provide either `image_path` or at least one `image_paths` entry.
  </Accordion>

  <Accordion title="sidekick_create_video">
    Create a short video generation job from a prompt. The result usually
    includes a video ID to poll or download.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `prompt` | string | yes | Video prompt |
    | `model` | string | no | Video model, default `sora-2` |
    | `seconds` | integer | no | Video duration in seconds; `4`, `8`, or `12`; default `4` |
    | `size` | string | no | `720x1280`, `1280x720`, `1024x1792`, or `1792x1024` |
    | `metadata` | object | no | Provider metadata |
    | `max_video_seconds` | integer | no | Maximum allowed video duration; `4`, `8`, or `12`; default `4` |
    | `timeout_seconds` | integer | no | Request timeout |
  </Accordion>

  <Accordion title="sidekick_get_video">
    Read generated video status or metadata.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `video_id` | string | yes | Video ID returned by `sidekick_create_video` |
    | `timeout_seconds` | integer | no | Request timeout |
  </Accordion>

  <Accordion title="sidekick_download_video">
    Download generated video content and save it locally. The result includes
    the saved path, MIME type, and byte count.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `video_id` | string | yes | Video ID returned by `sidekick_create_video` |
    | `timeout_seconds` | integer | no | Request timeout |
  </Accordion>

  <Accordion title="sidekick_delete_video">
    Delete a generated video.

    <Warning>
      This tool is registered only when Sidekick media video deletion is
      explicitly enabled for the mage-agent process.
    </Warning>

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `video_id` | string | yes | Video ID to delete |
    | `timeout_seconds` | integer | no | Request timeout |
  </Accordion>
</AccordionGroup>

---

## Pipelines

<AccordionGroup>
  <Accordion title="pipeline_list">
    List all pipelines in the project.

    **Arguments:** none

    **Example prompt:** *"List all my Mage pipelines"*
  </Accordion>

  <Accordion title="pipeline_get">
    Get details for a single pipeline.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `uuid` | string | yes | Pipeline UUID |
  </Accordion>

  <Accordion title="pipeline_create">
    Create a new pipeline.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `name` | string | yes | Pipeline name |
    | `type` | string | no | `python` (default), `streaming`, or `integration` |

    **Example prompt:** *"Create a new streaming pipeline called `realtime_events`"*
  </Accordion>

  <Accordion title="pipeline_update">
    Update fields on an existing pipeline.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `uuid` | string | yes | Pipeline UUID |
    | `name` | string | no | New name |
    | `description` | string | no | Updated description |
  </Accordion>

  <Accordion title="pipeline_delete">
    Delete a pipeline by UUID.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `uuid` | string | yes | Pipeline UUID |

    <Warning>Deletion is permanent and cannot be undone.</Warning>
  </Accordion>
</AccordionGroup>

---

## Blocks

<AccordionGroup>
  <Accordion title="block_list">
    List all blocks in a pipeline.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `pipeline_uuid` | string | yes | Pipeline UUID |
  </Accordion>

  <Accordion title="block_get">
    Get block details including source code.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `pipeline_uuid` | string | yes | Pipeline UUID |
    | `block_uuid` | string | yes | Block UUID |
  </Accordion>

  <Accordion title="block_create">
    Create a new block in a pipeline.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `pipeline_uuid` | string | yes | Pipeline UUID |
    | `name` | string | yes | Block name |
    | `type` | string | yes | Block type (see below) |
    | `language` | string | no | `python` (default), `sql`, `r`, `yaml`, `markdown` |
    | `content` | string | no | Initial source code |
    | `upstream_blocks` | array | no | UUIDs of upstream blocks |
    | `config` | object | no | Additional block configuration |

    **Supported block types:**
    `data_loader`, `transformer`, `data_exporter`, `sensor`, `callback`, `chart`, `conditional`, `custom`, `dbt`, `extension`, `global_data_product`, `markdown`, `scratchpad`

    <Note>
      If no `config` is provided, mage-agent automatically matches a block template for you. For `integration` pipelines, it also auto-detects available sources and destinations.
    </Note>

    **Example prompt:** *"Add a SQL transformer block to the `customer_etl` pipeline that filters rows where revenue > 1000"*
  </Accordion>

  <Accordion title="block_update">
    Update a block's metadata, dependencies, or configuration.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `pipeline_uuid` | string | yes | Pipeline UUID |
    | `block_uuid` | string | yes | Block UUID |
    | `payload` | object | yes | Fields to update, such as name, configuration, or upstream blocks |
    | `block_type` | string | no | Required when `payload.content` is present so mage-agent can delegate to the canonical code update workflow |

    <Note>
      For source-code edits, use `block_code_update` directly. It writes the
      block file first, triggers the block update path, and verifies readback
      before returning success.
    </Note>
  </Accordion>

  <Accordion title="block_code_update">
    Update a block's source code with the file-first workflow.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `pipeline_uuid` | string | yes | Pipeline UUID |
    | `block_type` | string | yes | Block type, such as `data_loader`, `transformer`, or `data_exporter` |
    | `block_uuid` | string | yes | Bare block UUID, without a `block_type/` prefix |
    | `content` | string | yes | New source code |
    | `payload` | object | no | Additional block metadata to include in the update callback |

    The tool resolves the block file, writes the file content, verifies file
    readback, triggers the normal block update callback/cache/file-version path,
    and verifies block readback. Generic `blocks` updates reject ambiguous code
    content mutations instead of constructing block URLs from mixed `pk` and
    query parameters.
  </Accordion>

  <Accordion title="block_templates_list">
    List available block templates.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `force_refresh` | boolean | no | Bypass the daily cache and re-fetch from cluster |

    Templates are cached for 24 hours at `~/.mage-agent/cache/block_templates.json`.
  </Accordion>
</AccordionGroup>

---

## Files

<AccordionGroup>
  <Accordion title="files">
    Read, write, upload, download, or record changes for project files.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `action` | string | yes | Resource action. File-transfer helpers include `upload`, `download`, and `record_local_change`. |
    | `remote_path` | string | depends | Absolute Mage project file path. Required for `upload` and `download`; optional for `record_local_change` when `local_path` is under the active project repo path. |
    | `local_path` | string | depends | Local source path for `upload` or `record_local_change`, or local destination path for `download`. |
    | `overwrite` | boolean | no | Allow an existing destination to be overwritten for supported actions. |
    | `verify` | boolean | no | For uploads, download the saved file and compare bytes against `local_path`. |

    <Note>
      In internal Mage Pro mode, if your assistant changes a project text file
      directly through local shell or filesystem access instead of a Mage API
      write, it should call `files` with `action="record_local_change"` for
      each changed file. This records the local UTF-8 text change through Mage
      so file versions are created for the project file.
    </Note>
  </Accordion>
</AccordionGroup>

---

## Runs & Logs

<AccordionGroup>
  <Accordion title="run_pipeline">
    Trigger a pipeline run.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `pipeline_uuid` | string | yes | Pipeline UUID |
    | `payload` | object | no | Optional run payload (variables, etc.) |

    **Example prompt:** *"Run the `daily_refresh` pipeline"*

    <Note>
      Use this for ad hoc whole-pipeline execution. To validate a fix for an
      existing failed or incomplete pipeline run, use
      `retry_pipeline_run_blocks` instead so Mage retries the original run's
      block runs instead of creating a separate run-once schedule.
    </Note>
  </Accordion>

  <Accordion title="retry_pipeline_run_blocks">
    Retry failed or incomplete block runs on the original pipeline run.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `pipeline_run_id` | string or number | yes | Original pipeline run ID |
    | `pipeline_uuid` | string | no | Pipeline UUID, used for log retrieval when the run response does not include it |
    | `from_block_uuid` | string | no | Block UUID to retry from, including downstream block runs in the same original run |
    | `retry_stream` | string | no | Integration stream to retry in the same original run |
    | `timeout_seconds` | number | no | Maximum time to wait before returning a pending result for the same pipeline run |

    **Example prompt:** *"Fix the failed block in run 123 and retry that same run"*
  </Accordion>

  <Accordion title="run_logs">
    Fetch logs for a specific pipeline run.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `pipeline_run_id` | string | yes | Pipeline run ID (returned by `run_pipeline`) |

    **Example prompt:** *"Show me the logs for the last run"*
  </Accordion>
</AccordionGroup>

---

## Sync

<AccordionGroup>
  <Accordion title="sync_project_to_local">
    Pull all remote project files to a local directory.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `local_project_dir` | string | no | Local directory path (default: `.mage-agent/local_sync`) |
  </Accordion>

  <Accordion title="sync_status">
    Show what has changed between local and remote.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `local_project_dir` | string | no | Local directory path |
  </Accordion>

  <Accordion title="sync_local_to_remote">
    Push locally changed files to the cluster.

    | Parameter | Type | Required | Description |
    |---|---|---|---|
    | `local_project_dir` | string | no | Local directory path |
  </Accordion>
</AccordionGroup>

---

## Mage Cloud Backend

Mage Cloud Backend tools are exposed only for installed-agent sessions that have
Cloud handoff credentials. These tools are generated from the Cloud Backend
resource inventory, so call `mage_tool_contract` before invoking a Cloud tool
when payload or query shape matters.

<AccordionGroup>
  <Accordion title="Cloud Backend contracts">
    Cloud Backend create and update requests use the same resource-root envelope
    that the backend API expects. Do not send flat mutation payloads.

    | Tool | Mutation payload root |
    |---|---|
    | `teams` | `team` |
    | `cloud_users` | `user` |
    | `team_memberships` | `team_membership` |
    | `invitations` | `invitation` |

    Role fields use Cloud Backend integer values: owner `0`, admin `1`,
    editor `2`, viewer `3`.

    For example, creating a team invitation uses:

    ```json
    {
      "action": "create",
      "identifiers": {
        "team_id": "42"
      },
      "payload": {
        "invitation": {
          "invitee_email": "user@example.com",
          "role": 3
        }
      }
    }
    ```

    `cluster_stats` requires `query.cluster_uuid`; `cluster_uuid` is not a path
    identifier for that tool.

    `cloud_users` supports `list`, `get`, and `update`. It does not expose
    `create`, because Cloud Backend treats `users.create` as the public signup
    path rather than a private team-admin user creation API.
  </Accordion>
</AccordionGroup>

---

## Error responses

All tool errors return a structured envelope:

```json
{
  "status": "error",
  "error": {
    "code": "AUTH_EXPIRED",
    "message": "Session token expired. Run `mage-agent login` to re-authenticate."
  }
}
```

| Error code | Cause |
|---|---|
| `CONFIG_INVALID` | Missing or malformed `~/.mage-agent/config.json` |
| `AUTH_REQUIRED` | No credentials found |
| `AUTH_EXPIRED` | 401/403 from cluster — run `mage-agent login` |
| `API_ERROR` | Cluster API returned an error |
| `TOOL_NOT_FOUND` | Requested tool name is not registered |
| `INTERNAL_ERROR` | Unexpected internal failure |

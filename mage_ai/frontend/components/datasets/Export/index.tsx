import FeatureSetType from '@interfaces/FeatureSetType';

type ExportProps = {
  featureSet: FeatureSetType;
};

function Export({
  featureSet,
}: ExportProps) {
  return (
    <div>
    </div>
  );
}

export default Export;

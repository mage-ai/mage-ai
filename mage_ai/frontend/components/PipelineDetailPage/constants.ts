export enum PageNameEnum {
  BACKFILLS = 'backfills',
  BLOCK_RUNS = 'block_runs',
  EDIT = 'edit',
  MONITOR = 'monitor',
  PIPELINE_LOGS = 'pipeline_logs',
  PIPELINE_RUNS = 'pipeline_runs',
  RUNS = 'runs',
  SETTINGS = 'settings',
  SYNCS = 'syncs',
  TRIGGERS = 'triggers',
}

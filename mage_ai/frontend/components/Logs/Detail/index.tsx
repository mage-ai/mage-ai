import LogType from '@interfaces/LogType';

type LogDetailProps = {
  log: LogType;
};

function LogDetail({
  log,
}: LogDetailProps) {
  return (
    <div>
      HEYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYY
    </div>
  );
}

export default LogDetail;

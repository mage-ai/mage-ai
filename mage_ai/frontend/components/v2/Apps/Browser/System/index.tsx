import { AppConfigType } from '../../interfaces';

type SystemBrowserProps = {
    app: AppConfigType;
};

function SystemBrowser({ app }: SystemBrowserProps) {
    return (
      <div>

        <h1>System Browser</h1>
      </div>
    );
}

export default SystemBrowser;

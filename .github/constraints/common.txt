# CI-only resolver hints to avoid backtracking / source builds

# requests stack
requests==2.31.0
urllib3<2            # widely compatible with Google/DBT libs still expecting <2
idna==3.7
charset-normalizer<4

# oauth stack
requests-oauthlib==1.3.1
oauthlib==3.2.2

# rx
reactivex==4.0.4